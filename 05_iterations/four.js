const subjects = {
    js: 'JavaScript',
    cpp: 'c++',
    rb: 'Ruby',
    swift: 'swift by apple'
}

for (const key in subjects) {
    // console.log(`${key} shortcut of ${subjects[key]}`);
}

const programming = ['js','php','java','python'];
for (const key in programming) {
    console.log(programming[key]);
}